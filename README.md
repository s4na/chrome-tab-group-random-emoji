# chrome-tab-group-random-emoji

Chrome extension that automatically assigns a random emoji to new tab groups.

## Install

1. `chrome://extensions/` を開く
2. 「デベロッパーモード」を有効にする
3. 「パッケージ化されていない拡張機能を読み込む」でこのディレクトリを選択

## How it works

新しいタブグループが作成されると、タイトルが空の場合にランダムな絵文字が自動的に割り当てられます。
